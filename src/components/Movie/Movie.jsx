const Movie = ({ movieData: { title } }) => {
  return (
    <div>
      <img src="" alt="there are poster image" />
      <p>Rating</p>
      <p>{title}</p>
    </div>
  );
};

export default Movie;
